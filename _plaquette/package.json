{
  "name": "pxlc-plaquette-builder",
  "version": "2.0.0",
  "private": true,
  "description": "Génère la plaquette PXLC en PDF A4 depuis plaquette.template.html + data.json (Chromium headless via Puppeteer)",
  "scripts": {
    "generate": "node generate-html.js",
    "build": "node generate-html.js && node build-pdf.js",
    "preview": "node render-previews.js"
  },
  "dependencies": {
    "puppeteer": "^23.11.1"
  }
}
