{
  "name": "pxlc-plaquette-builder",
  "version": "4.0.0",
  "private": true,
  "description": "Génère la plaquette PXLC en PDF A4 depuis plaquette.template.html + data.json + src/config/identity.ts (Chromium headless via Puppeteer)",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "generate": "node generate-html.js",
    "build": "node export-pdf.mjs",
    "preview": "node render-previews.js"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer": "^24.0.0"
  }
}
